services:
  - type: web
    name: punter-edge
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: API_KEY
        sync: false
      - key: DEBUG
        value: "False"
    healthCheckPath: /docs
